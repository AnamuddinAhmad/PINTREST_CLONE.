<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pinterest</title>
    <link rel="shortcut icon"
        href="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRZKYpfs-1Dx8ZUFLU5EvHtdUwE4bbb8S1cGg&s"
        type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet" />
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <% if(nav === true){ %>
        <div class="w-full px-10 py-3 flex justify-between bg-zinc-900 items-center">
            <h3 class="text-3xl text-red-600 font-semibold">Pinterest</h3>
            <div class="hidden md:flex gap-4 sm:gap-10 items-center text-white">
                <a class="px-4 py-1 rounded-md border border-white font-medium transition-all hover:bg-white hover:text-black" href="/profile">Profile</a>
                <a class="px-4 py-1 rounded-md border border-white font-medium transition-all hover:bg-white hover:text-black" href="/feed">Feed</a>
                <a class="px-4 py-1 rounded-md border border-white font-medium bg-red-700 hover:bg-red-600 transition-colors" href="/logout">Logout</a>
            </div>
            <div class="md:hidden flex items-center">
                <button id="hamburger" class="text-white focus:outline-none">
                    <i class="ri-menu-3-line text-3xl"></i>
                </button>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div id="mobileMenu" class="hidden flex flex-col bg-zinc-900 text-white px-10 py-5">
            <a class="py-2 border-b border-zinc-700 hover:bg-white hover:text-black transition-all" href="/profile">Profile</a>
            <a class="py-2 border-b border-zinc-700 hover:bg-white hover:text-black transition-all" href="/feed">Feed</a>
            <a class="py-2 hover:bg-red-600 hover:text-white transition-all" href="/logout">Logout</a>
        </div>
    <% } %>

    <div id="flash-message-container ">
        <% if (success.length > 0) { %>
          <div class="flash-message flash-message-success show mt-8">
            <%= success %>
          </div>
        <% } %>
      
        <% if (error.length > 0) { %>
          <div class="flash-message flash-message-error show mt-8 ">
            <%= error %>
          </div>
        <% } %>
      </div>
      
 
